server {
  listen 80 default_server;
  server_name _;
  root /root/meditech/site;
  index index.html;
  location / { try_files $uri $uri/ =404; }
  location / {
    try_files $uri $uri/ /index.html;
  }
}
